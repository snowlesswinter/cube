#include <stddef.h>
#include <windows.h>

#define __extension__
#define __attribute(x)
#define __pure
#define likely(x) (!!(x))
#define unlikely(x) (!!(x))
#define inline __inline
#define noinline
#define cache_align
#define MAP_FAILED	NULL